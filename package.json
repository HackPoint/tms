{
  "name": "application-name",
  "version": "0.0.1",
  "main": "index.html",
  "scripts": {
    "start": "http-server -a 127.0.0.1 -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "angular": "^1.5.7"
  },
  "devDependencies": {
    "http-server": "^0.9.0"
  }
}
